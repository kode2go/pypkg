
from setuptools import setup, find_packages

setup(
    name='pypkg',
    version='0.1',
    packages=find_packages(),
    install_requires=[],
    # Metadata
    author='Your Name',
    author_email='your@email.com',
    description='A simple library for adding numbers',
    url='https://github.com/kode2go/pypkg',
)
